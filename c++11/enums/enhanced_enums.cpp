#include <iostream>
#include <iomanip>
using namespace std;

enum Colors: unsigned char {red, green, blue};
//enum Status: uint8_t {pending, ready, unknown=9999}; // error!
enum Fruit {mango, banana, apple};

int main()
{

  return 0;
}
